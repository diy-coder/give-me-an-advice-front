type Motivacional @model @auth(rules: [{allow: public}]) {
  id: ID!
  nome: String
  descricao: String
  usuario: String
  ativo: String
}

type Conselho @model @auth(rules: [{allow: public}]) {
  id: ID!
  nome: String
  descricao: String
  usuario: String
  ativo: String
}

type Dica @model @auth(rules: [{allow: public}]) {
  id: ID!
  nome: String
  descricao: String
  usuario: AWSEmail
  ativo: Boolean
}
 